## What does this PR do?

<!-- Brief description of the change. -->

## Checklist

- [ ] Tests pass (`uv run pytest -q`)
- [ ] Linting passes (`uv run ruff check src/ && uv run ruff format --check src/`)
- [ ] Docs build cleanly (`uv run mkdocs build --strict`) if docs were changed
